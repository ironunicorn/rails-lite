<h1>INDEX all the Cats!</h1>
<ul>
<% @cats.each do |cat| %>
	<li>
		<a href="/cats/<%= cat.id %>">SHOW <%= cat.name %></a>
	</li>
<% end %>
</ul>
	<a href="/cats/new">New cat!</a>

<pre><%= flash["flash now"] if flash["flash now"] %></pre>
<pre><%= flash["flash next"] if flash["flash next"] %></pre>
